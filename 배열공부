#include <stdio.h>

int main(void)
{
    int n, x, i, j;

    scanf_s("%d %d", &n, &x);
    int a[n];

    for (i = 0; i < n; i++)
        scanf_s("%d", &a[i]);

    for (i = 0; i < n; i++)
    {
        if (a[i] < x)
            printf("%d", a[i]);
    }
    return 0;
}
