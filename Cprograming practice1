#include <stdio.h>

int main(void)
{
	int a, b;
	for (b = 9; b >= 1; b--)
	{
		for (a = 1; a<=9; a++)
		{
			if (a < b)
				printf(" ");
			else
				printf("%d", a);
		}
		printf("\n");
	}
	return 0;
}

여기에 이제 c프로그래밍한 연습문제들의 코드를 올릴 예정이야
